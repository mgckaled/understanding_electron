# Dev-only seed for cloud secrets (plano N-1-A, DN1A.1) — never committed with
# real values. Fills the secrets table on next `pnpm dev` boot, but only for a
# provider that has no key stored yet; a key written through the UI is never
# overwritten by this file.
GEMINI_API_KEY=
GLM_API_KEY=
