# Histórico de decisões e entregas — crivo

Changelog em ordem cronológica inversa. **Uma entrada curta por marco**, com link para o plano correspondente em [`plan/implemented/`](plan/implemented/) ou [`plan/archive/`](plan/archive/). O detalhe completo vive no plano linkado; aqui fica só o "o quê + por quê". Pendências ficam em [`ROADMAP.md`](ROADMAP.md) e [`plan/active/`](plan/active/).

> **Fonte única de história.** O `CLAUDE.md` e as skills apenas **apontam** para cá — não narram plano concluído nem repetem justificativa de decisão.

| Onde procurar | O quê |
|---|---|
| aqui | os **10 marcos mais recentes** e as decisões arquiteturais citáveis |
| [`HISTORY-archive.md`](HISTORY-archive.md) | todo marco mais antigo que os 10 — fila, não curadoria |
| [`ARMADILHAS.md`](ARMADILHAS.md) | erro diagnosticado, buscável **por sintoma** — saiu daqui em ago/2026 |
| [`DECISOES.md`](DECISOES.md) | índice tabular por sigla `D<id>`, derivado dos planos |

> ⚠️ **Não leia este arquivo na íntegra.** Ver [`CLAUDE.md`](../CLAUDE.md) § Protocolo de leitura da documentação.

---

## Como escrever uma entrada

Uma entrada nasce quando um plano sai de `active/` para `implemented/`. O formato é fixo:

```markdown
### Título do marco (mês/ano)
Origem: o que motivou. Entrega: o que passou a existir. Decisões: o que foi escolhido
e o que foi descartado, com o porquê. [`plan/implemented/NOME.md`](plan/implemented/NOME.md).
```

Três a oito linhas. Se precisar de mais, o detalhe pertence ao plano, não aqui.

⚠️ **A entrada nova entra no TOPO da seção, e a 11ª empurra a última para o [archive](HISTORY-archive.md) na mesma edição.** A lista é do mais recente para o mais antigo. Escrever no fim parece inofensivo — a contagem continua 10, o texto está lá, nada acusa —, mas põe o marco novo na posição do mais antigo, e a próxima entrada o arquiva. Aconteceu com o F-3-A: viveu **um commit** no `HISTORY.md`. A regra dos 10 já estava escrita; onde a nova entra, não.

**O que nunca pode faltar:** a alternativa descartada e o motivo do descarte. É o único conteúdo que não se recupera lendo o código depois — o código mostra o que foi feito, nunca o que foi considerado e rejeitado.

---

## Entregas (marcos)

### O-7 — Desempenho por modelo: rede+prefill, decode e tokens/s de entrada/saída (set/2026)
Origem: sétimo corte da trilha O — `promptTokens`/`evalTokens` já eram extraídos uniformemente dos três provedores (Ollama, GLM, Gemini) desde antes de a trilha O existir, nunca cronometrados nem persistidos. Entrega: tabela `performance_events` em `observatory.db` (schema `v2`, estendido por `v3` com `prompt_tokens`), `measureChatTiming` instrumentando `chat()` com três marcas de wall-clock (antes da chamada, no primeiro pedaço streamado, na resolução), e o painel Desempenho (segundo inquilino do grupo Atividade) com o resumo por `(service, model)`: rede+prefill, decode, tokens/s de entrada (prefill) e de saída (decode) — média/mediana/p90 — e carga de pico do modelo.

Decisões: tokens/s de saída usa piso de 5 tokens (DO7.8) — medido ao vivo contra o Ollama que uma resposta de 2 tokens produzia +12,6% de viés contra a taxa nativa (`eval_count ÷ eval_duration`); a correção óbvia (subtrair um token do numerador) foi testada e piorou o caso curto para −43%, então a saída é **descartar a amostra curta demais**, não corrigir a fórmula. Tokens/s de entrada (DO7.9) não precisa desse piso — vem de `promptTokens ÷ promptEvalDurationMs`, os dois nativos do provedor para a mesma chamada, sem o viés de "primeiro pedaço" que motiva o piso do decode.

**O mesmo tipo de contaminação foi achado e corrigido duas vezes na mesma sessão, em colunas diferentes, porque o Ollama reporta `load_duration` em toda chamada — não só na fria.** A carga do modelo virou **máximo**, não média (`maxLoadDurationMs`): uma carga fria de ~13,5 s e uma quente de ~6 ms no mesmo bucket, medidas ao vivo em sequência, produziriam uma média que não descreve nenhuma das duas. O tempo de rede+prefill (`avgNetworkPrefillMs`) tinha o defeito gêmeo — achado pelo advisor Opus na revisão da própria correção — e a saída foi subtrair `loadDurationMs` de `ttftMs` antes de agregar, verificado decompondo `ttftMs` ao vivo (`≈ carga + prefill + 24–66 ms de transporte local`). Nenhuma das duas correções foi deduzida sem medir.

Quatro rodadas de advisor (duas sobre o plano escrito, duas sobre a implementação) acharam, ao todo: um número de tamanho de arquivo desatualizado citado em dois lugares divergentes do `ROADMAP.md`; um handler já acima do teto de 150 linhas sem o plano prever a divisão (isolada em `main/observatory/chatTiming.ts`); uma decisão (DO7.5) se contradizendo com o passo de implementação sobre o que o canal devolve; e uma premissa falsa no texto original da DO7.4 — GLM/Gemini **streamam** de verdade numa chamada `format`-constrained, só o Ollama tem caminho separado; a razão real de `ai:propose` nunca gravar desempenho é estrutural (a função nunca chama `measureChatTiming`), não a diferença entre provedores. `check:fast` final: 143 arquivos, 1264 testes. Conferência ao vivo do usuário confirmou os três provedores lado a lado, com "—" honesto onde o dado nativo não existe (nuvem) e nas linhas gravadas antes desta extensão (backfill `NULL` de `prompt_tokens`, não bug). [`plan/implemented/O-7-desempenho-por-modelo.md`](plan/implemented/O-7-desempenho-por-modelo.md)

### O-6 — `observatory.db` e fluxo de eventos: o primeiro plano que grava (set/2026)
Origem: sexto corte da trilha O (§ 6) — a única fronteira que faltava: nenhum plano anterior persistia nada novo. Entrega: banco `observatory.db` separado do `crivo.db` (mesmo `openDatabase()`, escada de migração própria), tabela `events` (canal, duração, erro, id de domínio, timestamp), instrumentação no ponto único já existente (`ipcStats.wrap`), varredura de retenção configurável (7–90 dias, intervalo fechado) e o painel Eventos (grupo Atividade, primeira ocupação real do grupo desde a DO1.10).

Decisões: escopo estritamente fechado ao par banco+painel — Desempenho (O-7) e Privacidade (O-8) ficam para planos próprios, mesmo repartindo o arquivo; retenção lida na hora da consulta, não só no boot, para o cabeçalho do painel nunca anunciar uma janela que a varredura física ainda não aplicou.

Duas rodadas do advisor Opus e um teste ao vivo acharam, em sequência: o sink de eventos como parâmetro de construtor ficaria `undefined` para sempre (o singleton `ipcStats` nasce sem argumento, antes do composition root existir) — corrigido para setter mutável; o painel se auto-instrumentaria (`events:list` gravando a si mesmo) — excluído do sink; e, ao vivo, matar o Ollama não produzia linha de erro em lugar nenhum, porque canais que retornam `Result` (a maioria das falhas visíveis ao usuário, pela própria regra da skill `ipc`) nunca lançam exceção, e o `wrap` só via o `catch`. `resultError()` fechou a lacuna, e o mesmo critério passou a alimentar também o contador em memória do O-2 (Canais IPC), a pedido do usuário — conferido contra o plano do O-2 que nenhuma decisão ali excluía deliberadamente esse caso, era lacuna não testada. `check:fast`: 139 arquivos, 1237 testes. [`plan/implemented/O-6-observatoriodb-e-fluxo-de-eventos.md`](plan/implemented/O-6-observatoriodb-e-fluxo-de-eventos.md)

### O-5 — Uso de disco e cache do Chromium: o `userData/` se revela (set/2026)
Origem: quinto corte da trilha O (§ 6) — as duas fontes que restavam sem `observatory.db`: o cache HTTP do Chromium e a varredura de `userData/`. Entrega: dois canais (`session:cacheSize`/`clearCache`, `disk:usage`), `core/observatory/disk.ts` puro, e dois painéis no grupo Armazenamento — Cache do Chromium (Acessível) e Uso de disco (Caro, job cancelável, tabela crivo vs. runtime).

**Passo 0 mediu antes de decidir a forma do tipo.** A documentação do Electron não afirma que `session.getCacheSize()` corresponde ao total de bytes de `Cache/` no disco — só que é "o tamanho atual do cache da sessão". Medido ao vivo contra o `userData/` real: 127.452.694 B reportados vs. 134.664.478 B varridos, 5,36% de diferença, consistente com o índice/blocos esparsos do próprio backend de cache, não ruído. `DO5.3` adotou o ramo "bateram": `Cache/` nunca é caminhada, só medida por `getCacheSize()` — o mecanismo do painel de cache é o que torna o walk do painel de disco afordável, e é o motivo real de os dois nascerem no mesmo plano.

Decisões: **DO5.4** — o bucket do Chromium é um número só (`runtimeBytes`), nunca uma lista de pastas internas — enumerar `Code Cache/`, `GPUCache/`, `Local Storage/`… reintroduziria o ruído que a separação crivo/runtime existe para evitar; **DO5.5** — uma subpasta ilegível marca a entrada como `partial`, sem abortar o job inteiro, porque a métrica é best-effort por natureza; **DO5.8** — corrigida a própria classificação do inventário (§ 6): "Cache do Chromium" estava Trabalho Leve, e o critério do documento (canal novo = Moderado) não batia.

**O advisor Opus achou um defeito real numa segunda rodada, depois da conferência ao vivo do usuário ter passado sem incidente.** A primeira versão de `DiskUsagePanel` guardava o resultado em `useState`/`useAsyncAction`; como só o painel ativo monta (§ 4.2), trocar para outro painel e voltar descartava a medição inteira, voltando ao botão inicial como se nada tivesse rodado. `CapabilitiesPanel` nunca teve esse problema porque vive numa `useQuery({ enabled: false })` — o `QueryClient` sobrevive ao desmonte, ao contrário de `useState`. `DiskUsagePanel` foi reescrito no mesmo molde (**DO5.9**), com teste de regressão cobrindo o remount. Corrigido junto um `staleTime: 60_000` inventado em `ChromiumCachePanel`: `createQueryClient()` já fixa `staleTime: Infinity` globalmente, e um TTL de 60s era **mais fraco** que "cache obrigatório" (§ 5.1) — o oposto do pretendido. `check:fast`: 135 arquivos, 1218 testes. [`plan/implemented/O-5-uso-de-disco-e-cache-do-chromium.md`](plan/implemented/O-5-uso-de-disco-e-cache-do-chromium.md)

### O-4 — Capacidades: o primeiro painel caro, sob botão (set/2026)
Origem: quarto corte da trilha O (§ 6) — a única fonte Caro/Leve do inventário: o catálogo `ai:models` inteiro, três serviços, é sondagem de rede/subprocesso, não leitura de runtime como os três painéis anteriores. Entrega: painel Capacidades (grupo Estado), sem query automática — um botão dispara `useCapabilities`, uma `useQuery` só com `Promise.allSettled` sobre nove chamadas (disponibilidade + catálogo × 3 serviços, `secrets.has` × 2, `ai.loaded`), idade do resultado via `dataUpdatedAt` e `Intl.RelativeTimeFormat`. `LoadedModels` migra inteiro de Configurações para dentro do painel, sob o mesmo botão; `CAPABILITY_META` ganha `audio` (confirmado vivo no enum oficial do Ollama, `image` fica de fora por ser prototype). `CapabilityChip` promovido a `shared/ui/` (10º primitivo).

Decisões: **DO4.4** — o catálogo mostrado é o cru de `ai:models`, nunca `selectableModels` (que existe para o composer escolher **um** modelo, não para descrever o que está instalado); **DO4.7** — "Descarregar" chama o `refetch()` da própria sondagem, porque uma query `enabled: false` ignora `invalidateQueries` (confirmado via Context7) — trocado `useQueries` (oito relógios) por uma `useQuery` com `Promise.allSettled` manual (um `dataUpdatedAt` só).

**Três rodadas do advisor Opus antes da implementação** acharam, em ordem: uma contradição (a redação original furava a própria DO4.2 dando `LoadedModels` uma query própria com `staleTime: 0`); dois testes vacuamente verdadeiros (modal fechado nunca monta, então "não sonda nada fechado" provava o estado errado; e a sigla `'AU'` de `audio` coincide byte a byte com o fallback `UNKNOWN`, então testar a sigla não provava nada — o label é o que discrimina); e uma alegação falsa não verificada (`DraftPicker` supostamente já importava `CapabilityChip` — `grep` mostrou que só o citava num comentário; `ModelSelector` era o único chamador real, `CapabilitiesPanel` é o segundo, não o terceiro). A revisão pós-implementação achou mais um bloqueador real: o teste que provava que "Descarregar" relê a memória livre tinha sido perdido na migração para o Observatório — restaurado depois de confirmar ao vivo que `ConversationView` (não `RuntimePanel`, que não coexiste com `CapabilitiesPanel` — só um painel monta por vez) é o observador que segue montado atrás do modal, porque `Dialog` é irmão na árvore (D13.8), não rota.

**A conferência ao vivo do usuário achou dois defeitos que nenhum teste alcançava, porque exigem conhecimento externo ao código.** `nomic-embed-custom:latest`, `gemma3-4b-custom:latest` e os demais pares `-custom:latest` não são um segundo modelo instalado — são Modelfile clones do mesmo peso, feitos para o mill.tools (app irmão que também fala com este Ollama). DO4.5 originalmente decidia mostrar embedder e clone lado a lado; revertida — o composer já resolvia exatamente este caso (`selectableModels`/D15.11 descarta a variante cujo pai está instalado), extraída a parte reaproveitável para `dropRedundantVariants` em `core/ai/models.ts`. Generalizado depois, a pedido do usuário: todo modelo `embedding` sai da tabela do próprio serviço e vive só na seção Embedder, para qualquer provedor (Ollama hoje, Gemini quando tiver embedder opt-in). Segundo achado, de layout: cada tabela dimensionava colunas pelo próprio conteúdo, cabeçalho quebrando por palavra em janela estreita — corrigido com `table-fixed`/`colgroup` de larguras idênticas entre tabelas, `overflow-x-auto` + `min-w-[640px]` em vez de espremer texto. `check:fast`: 130 arquivos, 1200 testes. [`plan/implemented/O-4-capacidades-o-primeiro-painel-caro.md`](plan/implemented/O-4-capacidades-o-primeiro-painel-caro.md)

### O-3 — Os dois motores se descrevem: painéis Motor DuckDB e Banco de dados (ago/2026)
Origem: terceiro corte da trilha O (§ 6 da fundamentação) — as duas fontes Grátis/Barato-Moderado, já Disponíveis, que restavam no inventário: o motor DuckDB em vigor e o `crivo.db`. Entrega: dois canais (`dataset:engineInfo`, `database:info`), dois painéis novos — Motor DuckDB no grupo Estado, Banco de dados no grupo Armazenamento (o primeiro painel daquele grupo desde o O-1).

Decisões: **DO3.2** — `dataset:engineInfo` retorna `Result`, ao contrário de `dataset:queueDepth` do O-2, porque faz round-trip pelo worker e herda o mesmo risco de worker morto; **DO3.3** — `database:info` não retorna `Result`, leitura do `crivo.db` já aberto sem modo de falha que a UI precise distinguir; **DO3.4** — lista de tabelas sempre derivada de `sqlite_master`, nunca hardcoded, provado com uma tabela criada em teste já que `sqlite_master` não é sabotável diretamente.

Revisão do advisor Opus achou dois defeitos reais no resultado: `formatSize` (GB fixo) fazia memória por tag e tamanho do banco — tipicamente MBs — aparecerem como "0,0 GB", indistinguível de vazio; corrigido com `formatBytes` (escala automática B/KB/MB/GB) só para os dois casos pequenos, os sete consumidores existentes de `formatSize` ficaram intocados por dependerem da escala fixa em GB para comparabilidade entre si (RAM, peso de modelo). A conferência ao vivo do usuário achou um bug que nenhum teste automatizado alcançava: `extension_directory` nunca era definido, ficava fora de `allowed_directories`, e `duckdb_extensions()` sonda esse caminho para reportar `installed` — batendo em `enable_external_access = false` com Permission Error. A sondagem ao vivo do passo 1 não pegou porque rodava contra uma `DuckDBInstance` nua, sem a sequência de startup restrita do worker real. Corrigido apontando `extension_directory` para `tempDirectory`, mesma posição de ordem que `allowed_directories`/`temp_directory` já ocupavam (`enable_external_access` rejeita mudar qualquer um dos três depois de desligado). `check:fast`: 1187 testes. [`plan/implemented/O-3-os-dois-motores-se-descrevem.md`](plan/implemented/O-3-os-dois-motores-se-descrevem.md)

### O-2 — IPC, jobs e a fila do worker: o risco documentado vira mostrador (ago/2026)
Origem: segundo corte da trilha O (§ 6 da fundamentação) — três fontes Grátis/Moderado, já Disponíveis, que pedem canal novo sem persistência nova. A escolha do corte foi revisada contra um relatório externo (`notes/observatory/report.md`), que confirmou a tese central mas errou ao dizer corrigido o risco do worker morto no meio da fila — `ARMADILHAS.md` mostra que não foi. Entrega: três canais (`app:ipcStats`, `job:list`, `dataset:queueDepth`), um núcleo puro para os contadores de IPC, e **um** painel — não três — agregando as três leituras.

Decisões: **DO2.1** — um painel, três blocos, não três entradas na sidebar (precedente do próprio mill.tools, e resposta ao risco de crescimento horizontal que o relatório citava em si mesmo); rejeitado `refetchInterval` do TanStack Query — um painel que vira "acompanhar" deveria sair do modal, não ganhar polling (§ 4.5 da fundamentação); **DO2.6** — a fila mede profundidade agora, não tempo por requisição. Dois achados do advisor Opus antes de virarem defeito: um decremento de contador (`depth--`) numa promessa derivada separada geraria `unhandledRejection` exatamente quando o worker morre no meio da fila — corrigido antes da implementação; e o painel coalescia leitura pendente/com erro em zero, violando "métrica ausente não é zero" (DO1.8) — corrigido com `ViewState` explícito por bloco. `check:fast`: 1167 testes; conferência ao vivo: 29 canais distintos exercitados em uso real, 0 falhas. [`plan/implemented/O-2-ipc-jobs-e-fila-do-worker.md`](plan/implemented/O-2-ipc-jobs-e-fila-do-worker.md)

### O-1 — A casca do observatório, e os dois painéis que não custam nada (ago/2026)
Origem: primeiro plano da trilha O (Observatório), nascida do brief em `notes/observatory/brief.md` e da leitura do observatório do mill.tools — o corte escolhido são os únicos painéis Grátis que também leem estado que já existe. Entrega: `features/observatory/` — gatilho no rodapé da sidebar, modal largo com sidebar própria e registro de painéis (`panels.ts`), carregamento tardio do módulo inteiro, e dois painéis: Runtime (absorve o `Versions` que vivia dentro de Configurações) e Processos (`app:processes`, canal novo sobre `app.getAppMetrics`).

Decisões: **DO1.2** — dois painéis, não um, porque a invariante "só o painel ativo monta" (§ 4.2) é intestável com um só; **DO1.4** — `Versions` muda de casa (Configurações é o que se muda, Observatório é o que se olha), mudança visível ao usuário registrada como decisão antes de existir código; **DO1.7** — bytes em todo o contrato, porque `ProcessMetric.memory` do Electron vem em kilobytes contra os bytes do resto; **DO1.10** — sidebar do modal derivada de `panels.ts`, nunca escrita à mão (o painel de disco do mill.tools envelheceu justamente na parte escrita à mão). Achados ao vivo: `residentSet` não existe, o campo real é `workingSetSize`; a provocação "importar `electron` por valor" dá falso negativo — o que reprova é `Function.length`, achado que subiu para `ARMADILHAS.md`. `check:fast`: 1146 testes. [`plan/implemented/O-1-a-casca-do-observatorio.md`](plan/implemented/O-1-a-casca-do-observatorio.md)

### Manutenção de documentação: a seção "Operações de arquivo" no `CLAUDE.md` (ago/2026)
Origem: pedido do usuário para fixar, num lugar único, a regra "nunca passe para o LLM o que uma ferramenta de shell pode filtrar primeiro" — até então implícita na prática de cada sessão e espalhada entre skills, sem parâmetro numérico para este projeto. Entrega: nova seção no `CLAUDE.md`, entre "Organização da documentação" e "Stack fixada", com a tabela das cinco operações (ler, escrever, buscar, verificar, mapear) e, para cada uma, o threshold concreto — quando filtrar um arquivo de código ou log antes de `Read`, quando `sed -i`/heredoc/`>>`/patch substituem `Edit`, o funil arquivo → linha → bloco para busca (`ctags` e `fd` checados e ausentes nesta máquina), os filtros de saída de `pnpm test`/`typecheck`/`lint`/`dev`/`list`, e como mapear símbolo e camada sem violar a tabela de importação entre `shared`/`core`/`main`/`workers`/`preload`/`renderer`.

**Descartado: uma skill nova** (`file-ops` ou similar) em vez de seção no `CLAUDE.md`. Skill só carrega quando invocada ou quando o gatilho da descrição casa — e a regra "filtre antes de entregar ao LLM" precisa valer em **toda** sessão, inclusive na primeira leitura, antes de qualquer skill ser considerada. Só o `CLAUDE.md` tem essa garantia, ao custo de aprofundar um teto já estourado (abaixo).

Sem plano — mudança de documentação pura, sem canal IPC nem dependência nova. Duas linhas entraram depois, a pedido do usuário após consultar o Opus advisor sobre outras alavancas de economia: threshold de ~200 linhas para aceitar saída bruta de qualquer ferramenta (em "Verificar"), e quando usar fork/subagent para levantamento aberto cujo resultado intermediário não precisa voltar ao contexto principal (em "Buscar") — a segunda mira o que de fato pesa numa sessão (mensagens, não o arquivo). Custo assumido: o próprio `CLAUDE.md` cresceu de 31,7 para 39,2 kB, mais alto ainda acima do teto de ~25 KB já estourado — o desmembramento segue pendente no [`ROADMAP § 2`](ROADMAP.md).

### E-2 — O rascunho aceita código: um bloco da resposta vira arquivo (ago/2026)
Origem: pergunta do usuário sobre admitir arquivo de código como anexo, que ele mesmo dissolveu ao trocar a fonte — *"é o modelo que vai dar o código na resposta"*. Entrega: o botão no cabeçalho de cada bloco de código manda aquele trecho para o painel de rascunho, com gramática por linguagem no editor e na prévia, numeração de linha, e exportação verbatim com a extensão que a linguagem pede. Dois planos, [E-2-A](plan/implemented/E-2-A-o-rascunho-aceita-codigo.md) (o dado e o caminho) e [E-2-B](plan/implemented/E-2-B-cada-linguagem-no-seu-dialeto.md) (o dialeto).

**Não nasceu um terceiro inquilino de painel, e a proposta que venceu foi do dono do projeto.** O desenho passou por três formas: inquilino próprio com contador e atalho; inquilino na interface com um dono no dado (minha proposta intermediária); e — a adotada — **código é um rascunho de outro dialeto**. O argumento: nos quatro eixos que separam artefato de rascunho (origem, persistência, edição, exportação) um trecho de código cai inteiro do lado do rascunho, e a única diferença é o dialeto do conteúdo. A distinção viaja **no item** (um chip de linguagem), não no painel. Isso poupou as ~444 linhas de `DraftPanel`/`Picker`/`Footer`/`Count`/`Editor`, que servem os dois tipos sem duplicação. `kind`/`language` são **duas** colunas porque uma cerca pode ser código **sem** nomear linguagem (D11.5) — com uma só, esse caso seria indistinguível de prosa.

**Tratar código como markdown destrói código, e a lição teve de ser reaprendida do outro lado.** O E-1-E já registrara isso na serialização (`remark-stringify` escapando e trocando indentação por `&#x20;`). O E-2-A reintroduziu a irmã na **renderização**: a prévia rodava o código pelo `MarkdownMessage`, que junta linhas consecutivas num parágrafo e lê quatro espaços como bloco aninhado — um corpo de classe voltava como prosa mais um bloco, sem as quebras de linha. E o editor tinha a outra metade, pela mesma regra dos quatro espaços: `tags.monospace` pintava o corpo indentado inteiro de verde. Conserto: nenhuma das duas abas passa o código por markdown. Terceira aparição, do lado da **saída**: `.txt`/`.docx`/`.pdf` passam todos por `toBlocks`, então código não escolhe formato — sai verbatim, e o `FormatPicker` com uma opção só vira rótulo, pela regra que o `DraftPicker` já aplicava a um rascunho só.

**Um `Highlighter` só serve o editor e a prévia, e isso é o conserto de um defeito relatado.** `classHighlighter` (de `@lezer/highlight`, já declarada) emite classes `.tok-*` e não carrega estilo; `syntaxHighlighting` o usa no editor, `highlightCode` na prévia, e as regras vivem uma vez em `base.css` — que o `guard` varre, provado por provocação. Duas listas de estilo seriam a forma de reintroduzir a divergência de cor entre as abas que o usuário havia reportado. Gramáticas por `@codemirror/legacy-modes`, não pelos `lang-*` oficiais: não existe `lang-any`, e um arquivo de modo cobre várias linguagens.

⚠️ **Um defeito que o próprio plano cria é responsabilidade dele.** `hasDraftOf` decidia o botão da resposta perguntando só pela procedência; um trecho de código nasce com o **mesmo** `sourceMessageId`, então mandar um bloco marcava a resposta inteira como rascunhada. Invisível: hoje era impossível uma mensagem ter dois rascunhos, logo **nenhum teste existente falhava**. O botão do **bloco** teve o defeito simétrico, achado ao vivo — sem estado nenhum, a mesma cerca virava rascunho sem limite; a chave inclui o `content` porque uma cerca não tem id e dois blocos da mesma resposta compartilham a mensagem.

⚠️ **Estimativa de bundle errou por 2,2× e o número do build é que vale.** O plano prometia +117,9 kB; o build cresceu **+261,75 kB**. Remedido com os imports reais, os 27 modos custam 149.936 B — a sonda media 14 arquivos, a implementação usa 17; **os ~112 kB restantes ficaram não atribuídos**, registrado como tal em vez de arredondado.

⚠️ **Consertar CSS por sintoma não converge — duas tentativas minhas antes da causa.** O código aparecia à esquerda dos números ao rolar. Tentei fundo opaco; depois empilhei `paddingLeft: 0`. A causa, lida no fonte do pacote: o gutter já é fixo (`gutters({fixed})` é opt-out) e o plugin aplica `position: sticky` inline com `insetInlineStart: 0` — e **`left: 0` num sticky é relativo à caixa de _padding_ do contêiner de rolagem**, então o `padding` do `.cm-scroller` ancorava o gutter 24px adentro e deixava a faixa descoberta. Conserto: só padding vertical no scroller. Junto veio o segundo pedido, que remove a *oportunidade* em vez da causa — o CodeMirror **não tem quebra por coluna** (verificado), então `lineWrapping` + `max-width: 90ch`. `check:fast`: 1130 testes, 116 arquivos. [`plan/implemented/E-2-B-cada-linguagem-no-seu-dialeto.md`](plan/implemented/E-2-B-cada-linguagem-no-seu-dialeto.md)

### E-1-F — O `.pdf` pelo motor que já estava no app, e a trilha E fecha (ago/2026)
Origem: último item desabilitado do `FormatPicker`, e a única bifurcação técnica que a trilha ainda tinha. Entrega: `.pdf` de ponta a ponta por `webContents.printToPDF` numa janela offscreen, **zero dependência nova**, com número de página no rodapé. O seletor fica sem nenhuma opção desabilitada pela primeira vez.

**A bifurcação era `pdf-lib` contra `printToPDF`, e o usuário dissolveu a objeção que restava.** O `pdf-lib` não é uma dependência (é ele, `@pdf-lib/fontkit` e uma fonte vendorizada) e **não pagina** — o `ESCOPO` já registrava isso ao escolhê-lo, e resposta de modelo é prosa longa de tamanho arbitrário. Contra o `printToPDF` só sobrava bifurcar o pipeline; ao perguntar se o `.pdf` não podia sair *"no mesmo esquema do `.docx`"*, ele apontou a saída: **gerar o HTML a partir do `Block[]`**. O `.pdf` virou o **terceiro renderizador** do mesmo mapeamento, e ainda o mais testável dos três — HTML é string, então o nível 1 compara o documento por igualdade, que é justamente o que a DE1E.9 cobrou e o `.docx` não permitiu.

**Segurança: gerar HTML de texto de modelo parece contrariar a D12.2 e não contraria.** Ela proíbe *transformar* texto do modelo em HTML; aqui todo run é **escapado** e só saem as nossas tags — inclusive o nó `html` do markdown, que a DE1E.7 já converte em texto e o escape alcança de graça. Segunda tranca: `default-src 'none'` no documento gerado. **E ela é real, não decorativa** — a orientação geral diz que CSP em `<meta>` inserida depois do parse é ignorada; medido com `securitypolicyviolation`, está **ativa** por este caminho.

⚠️ **Uma palavra ambígua custou uma investigação inteira e pagou por ela.** O relato *"a paginação não veio"* foi lido como quebra em páginas, quando era **numeração**. Reproduzir antes de consertar impediu o estrago — as sondas paginavam, e o rascunho reexportado deu 3 páginas completas —, e a investigação pelo caminho errado achou **dois defeitos reais**: o documento inteiro renderizava em **modo quirks** (o `data:` inicial não tinha doctype, e `innerHTML` não muda compat mode depois), e uma **medição minha era falsa**.

⚠️ **A medição falsa é a lição que sobrevive ao plano.** Eu havia registrado, em fonte e em decisão, que o `data:` URL falha "entre 128 e 192 KB". Era artefato da sonda: ela destruía a **única** janela, o que dispara `window-all-closed` e encerra o app por padrão — então a **segunda** janela falhava sempre, em qualquer tamanho, e eu li ordem como tamanho. Com uma âncora viva (o que a `mainWindow` é em produção): 1 MB passa, 2 MB morre com `ERR_INVALID_URL`, e a carga em dois tempos não tem teto a 4 MB. **Uma medição que varia duas coisas ao mesmo tempo mede a que não se está olhando.** `check:fast`: 1047 testes, 113 arquivos. [`plan/implemented/E-1-F-o-rascunho-vira-pdf.md`](plan/implemented/E-1-F-o-rascunho-vira-pdf.md)

### Decisão: erro que atravessa o IPC é dado, não exceção
Exceção não sobrevive ao `structured clone` do Electron. Se um handler lança, o `ipcRenderer.invoke` rejeita com um `Error` genérico prefixado com `Error invoking remote method`, e a classe, as propriedades customizadas e o stack original se perdem. Um `QuerySyntaxError { line, column }` chegaria ao React como texto inútil. Por isso toda operação que atravessa a fronteira retorna união discriminada. **Contrapartida deliberada:** payload fora do schema **lança** — é bug de programação, e um erro mutilado no console durante o desenvolvimento é a resposta certa. [`plan/implemented/02-contrato-ipc.md`](plan/implemented/02-contrato-ipc.md)

### Decisão: pipeline de passos, não SQL-first
A composição de uma transformação vive numa lista ordenada de operações que compila para SQL, e não numa consulta única. Descartado o modelo query-first do mill.tools: ele serve para *perguntar* ao dado, e aqui o trabalho é *tratar* o dado — iterativo, com o resultado de um passo mudando o que se quer no seguinte. Desfazer vira remover um passo, cada passo é inspecionável, e a sequência é uma receita reaplicável a outro arquivo. Descartada também a grade editável célula a célula: exigiria estado mutável, desfazer por diff e escrita através da virtualização. [`ESCOPO.md`](ESCOPO.md)

### Decisão: dois verbos — NL→SQL para *perguntar*, NL→passo para *tratar*
Decisão revista **duas** vezes, e as três versões valem porque cada uma foi invalidada por um fato novo, não por preferência. **(1)** A intenção original era portar o `nl2sql` do mill.tools. **(2)** O modelo de pipeline a invalidou: gerar SQL opaco contorna o que dá valor ao passo — editável e inspecionável, o que SQL de trinta linhas não é —, e a IA passou a produzir *a mesma estrutura de dados que a interface produz*. **(3)** A virada para chat trouxe para dentro do escopo um verbo que antes não existia: *perguntar*. Uma média por cidade é consumida uma vez e não faz sentido como passo 4 de uma receita de limpeza; uma deduplicação por CPF é reaplicável e não faz sentido como consulta descartável. O `ESCOPO.md` já continha a distinção sem tirar a consequência — ele descartava SQL-first reconhecendo que "funciona bem para perguntar ao dado". Forma final: uma proposta é união discriminada (`kind: 'query'` ou `kind: 'steps'`), o modelo roteia e o app valida. Nada da versão (2) foi perdido; ela deixou de ser a resposta única. [`ESCOPO.md`](ESCOPO.md) · [`plan/active/09-camada-de-ia.md`](plan/active/09-camada-de-ia.md)

### Decisão: um schema zod restringe a geração **e** valida a resposta
O catálogo de operações é um schema zod em `core/pipeline/`. Dele saem duas coisas do mesmo lugar: `z.toJSONSchema()` alimenta o campo `format` do `/api/chat` do Ollama, que restringe a geração no nível da gramática — o modelo não consegue emitir token fora do formato —, e o mesmo schema faz o `.parse()` da resposta. É a convenção de fonte única aplicada à fronteira da IA, e é o que torna um modelo de 4B viável como caminho principal: sem gramática restrita, JSON livre de um 4B falha com frequência que inviabiliza. O projeto já tinha zod 4 por outro motivo (validação de payload de IPC, fase 02), então isto custou zero dependência. **A gramática garante a forma, nunca o sentido** — passo sintaticamente válido e semanticamente errado continua possível, e é por isso que a revisão humana antes de executar não é opcional.

### Decisão: persistência em `node:sqlite`, e não relacional descartado
`node:sqlite` está **dentro do binário do Electron** (verificado no Electron 42.8.0 desta máquina: Node 24.18.0, SQLite 3.53.1, estabilidade 1.2 desde o Node 24.15.0, sem flag) — zero pacote npm, zero módulo nativo, zero `@electron/rebuild` a cada bump de Electron, e **não dispara o gatilho do `shamefullyHoist`** que o [`ROADMAP § 2`](ROADMAP.md) mantém aberto. Descartado banco não relacional, e o instinto de que seria "mais rápido e mais fácil" merece o registro do porquê não: numa base documental a conversa é um documento, e **cada mensagem nova reescreve o documento inteiro** — aos 200 turnos são centenas de KB regravados por resposta, contra um `INSERT` de uma linha; a sidebar quer "títulos ordenados por `updated_at`", que é projeção relacional; e *schemaless* não elimina migração, apenas a move para o caminho de leitura, onde nunca mais sai. Descartados nominalmente `classic-level` (módulo nativo), `lowdb`/`electron-store` (JSON, reescrita do arquivo inteiro), `@seald-io/nedb` (índice inteiro em RAM no boot) e PouchDB/RxDB (máquina de sincronização sem uso aqui). A flexibilidade de documento que se quer de fato — metadados que variam por mensagem — mora numa coluna JSON, consultável por `json_extract`. Bônus do stack: o DuckDB lê arquivo SQLite via `ATTACH`, então o histórico fica analisável pelo próprio app; um banco de documentos seria uma ilha. **Contrapartida:** a API é síncrona e roda no main, o que contraria a D9.1 na letra — aceitável porque listar conversas e inserir mensagem são operações indexadas de microssegundos. O gatilho que reabre isto está no [`ROADMAP § 2`](ROADMAP.md).

### Decisão: SQL gerado por modelo roda com o motor restringido, não com o texto inspecionado
A versão (2) da decisão dos dois verbos evitava SQL gerado justamente para não precisar de um `ensureSelect` defendendo por expressão regular contra DML, `COPY` e `ATTACH` no texto. Com o verbo *perguntar* de volta, o custo voltaria — exceto que a defesa correta não é textual: o DuckDB documenta `allowed_directories`, `enable_external_access = false`, `autoinstall_known_extensions = false` e, a peça que fecha o buraco, `lock_configuration = true`, que impede a própria consulta gerada de reverter o resto com um `SET` na primeira linha (`allowed_configs` libera nominalmente o que precisa continuar ajustável, como `memory_limit` e `threads`). A garantia passa a ser do motor. Forma da lição: **quando o adversário é texto gerado, procure a restrição na camada que executa, não na que lê** — regex sobre SQL é lista de negação a manter, com a mesma falha estrutural já registrada para o `files` do `electron-builder`. **A ordem entre os `SET` é parte da garantia, não detalhe de estilo** — `allowed_directories` e `temp_directory` têm que ser setados antes de `enable_external_access = false`, achado ao vivo no plano 18-A, ver § Armadilhas. [`ESCOPO.md`](ESCOPO.md) · [`plan/implemented/18-A-motor-e-worker.md`](plan/implemented/18-A-motor-e-worker.md)

### Decisão: RAG entra por capacidade, nunca por eficiência — e o cache de prefixo é o motivo
O instinto vindo de API de nuvem diz que indexar um documento anexado é mais eficiente que mandá-lo inteiro, porque lá se paga por token **a cada chamada**. Localmente a economia se inverte: o Ollama reaproveita o prefixo do prompt, então o documento inteiro é token novo **uma vez** — enquanto os trechos recuperados por RAG **mudam a cada pergunta** e são token novo para sempre. Medido nesta máquina com o `ESCOPO.md` (19 kB, 4.986 tokens): documento inteiro custa 200 s no primeiro turno e ~3 s nos seguintes; RAG custa 39 s de indexação e ~27 s **por turno** (0,5 s de embedding da consulta mais o prefill dos trechos). O empate fica em ~6 turnos, e a partir daí a distância só cresce a favor do documento inteiro. Indexar em si é rápido — o `nomic-embed-text` fez 6.998 tokens a **~180 tok/s**, sete vezes o prefill de 25 tok/s do `gemma3:4b` —, e é justamente por isso que a conclusão surpreende: o gargalo nunca foi indexar. **Forma da decisão:** RAG entra quando o documento **não cabe** (acima de ~8k tokens o prefill passa de cinco minutos e não é tolerável nem uma vez) ou quando a busca é **entre** documentos — *"em qual arquivo eu vi aquela tabela?"* —, que o prefixo não responde e que já é a fatia 5 do [plano 09](plan/active/09-camada-de-ia.md). Lição de forma, maior que o caso: **antes de portar uma otimização, confira se a premissa de custo dela vale no ambiente novo** — aqui, "token custa toda vez" é verdade na nuvem e falso na máquina local.

### Decisão: chamada de modelo no main; cálculo sobre vetores no `utilityProcess`
Contraria a intuição criada pelo raciocínio do DuckDB, e a distinção é o ponto. O DuckDB precisa de processo separado porque é limitado por CPU e bloqueia a thread; uma requisição HTTP ao Ollama ou ao Gemini é limitada por entrada e saída, e o `fetch` assíncrono devolve o controle ao *event loop*. Já recuperação de RAG — cosseno sobre matriz, BM25, MMR — **é** limitada por CPU e acompanha o DuckDB. A fronteira é *quem bloqueia a thread*, não *o que parece pesado*. [`plan/active/09-camada-de-ia.md`](plan/active/09-camada-de-ia.md)

### Decisão: segredo é de mão única — o renderer escreve, nunca lê
O contrato de credenciais tem `set`, `status` e `clear`, e não tem `get`. Chave que chega ao renderer entra em estado do React, aparece no DevTools e vai parar em relatório de erro. Armazenamento é `safeStorage` (DPAPI no Windows) em `app.getPath('userData')` — **nunca `.env` na raiz**: o mill.tools pode fazer isso porque roda do fonte; app empacotado que lê `.env` da raiz distribui a chave junto com o instalador. Regra fixada antes de existir a primeira chave. [`plan/implemented/03-sandbox-e-seguranca.md`](plan/implemented/03-sandbox-e-seguranca.md)

### Decisão: SOLID entra parcial — ISP e DIP sim, OCP não
SOLID nasceu em OOP de classes, num mundo onde a biblioteca era distribuída em binário e não se podia editar o fonte. **ISP** é adotado — é o argumento contra expor um `invoke(canal, args)` genérico no preload. **DIP** é adotado na forma nativa da linguagem: parâmetro de função tipado, sem container de injeção, que é imposto de Java numa linguagem com função de primeira classe. **OCP é recusado**: somos donos do repositório e temos git, e ponto de extensão especulativo é retrabalho antecipado. SRP já está coberto pela régua de coesão; LSP é quase inaplicável com união discriminada. [`plan/implemented/00-visao-geral.md`](plan/implemented/00-visao-geral.md)

### Decisão: flexibilidade é forma de dado e slot, nunca ponto de extensão
Complementa a recusa do OCP acima, e existe porque as duas coisas se confundem com facilidade. O aplicativo vai crescer além do escopo fixado na [virada de ago/2026](HISTORY-archive.md#a-virada-o-chat-vira-a-porta-de-entrada-ago2026) — descrição de imagens por VLM, tela de configurações com gerência de chaves, provedores de nuvem —, e a pergunta não é *"vou precisar disso?"*, é a régua da fase 00: **se eu precisar, mudo um arquivo ou trinta?** Três camadas, com respostas diferentes: **(1) forma do dado que atravessa camadas** — `Message`, capabilities do modelo, versão do esquema — decide-se **agora**, porque retrofitar toca `shared/ipc.ts`, preload, renderer, main, as linhas já gravadas e a migração; **(2) costuras e slots** — casca por região, `ChatFn` por parâmetro, um construtor de contexto em `core/` — decide-se agora porque **custa zero**; **(3) features de verdade** — VLM, configurações, nuvem — constrói-se quando existirem, sem uma linha de antecipação. A distinção que impede isto de ser OCP disfarçado: **um slot não é ponto de extensão, é a recusa a fixar.** Um `AppShell` que recebe `main` como slot não tem uma linha a mais que um que renderiza a conversa direto — é o mesmo código, menos acoplado, e o projeto já pratica isso desde a fase 02, quando `openExternal` nasceu recebendo a função de abertura por parâmetro. **Aplicação que fechou o argumento:** modalidade nova — o `gemma3:4b` já instalado declara `capabilities: ['completion', 'vision']`, ainda que o caminho prático para imagem provavelmente seja um modelo de nuvem dedicado, o que só reforça a necessidade de o par `service`/`model` ser dado de primeira classe — cobra exatamente a mudança que os artefatos já cobravam: `Message` deixa de ser `content: string` e passa a ser **lista de partes tipadas**, traduzida para cada provedor por função pura em `core/`, no mesmo lugar onde já mora a fronteira de privacidade dos três níveis. Uma decisão, três consumidores, nenhum caminho paralelo. **Recusados explicitamente:** sistema de plugin ou registro de features, hierarquia de provedores além do `ChatFn`, tabela genérica de entidade no SQLite, e camada de "workspace" antes de existir o segundo tipo de coisa. O que substitui todos eles é infraestrutura e não abstração: **`PRAGMA user_version` com escada de migração desde a v1** — com ela, tabela nova é operação normal; sem ela, todo tipo novo é uma crise. [`ESCOPO.md`](ESCOPO.md) · [`plan/active/README.md`](plan/active/README.md)

### Decisão: um hook público sobrevive à troca de fonte quando o TIPO que ele devolve tem nome próprio
A D13.2 prometeu que trocar o estado em memória por um cache de servidor mudaria "o corpo de dois hooks, não quatro componentes". A promessa se cumpriu — a fase 14 tocou três hooks e **zero componentes**, com os 33 testes de nível 2 do plano 13 passando só com o provider acrescentado ao envoltório —, e vale registrar **o que a fez se cumprir**, porque não estava no plano e por pouco não aconteceu. Com armazenamento relacional, a conversa deixa de carregar suas mensagens: a sidebar lista linhas e a transcrição é uma segunda leitura. O caminho intuitivo era criar um `ConversationSummary` ao lado do `Conversation` existente — e ele **quebraria** `ConversationList`, que anota `Conversation` na sua `RowProps`. O caminho que funciona é o inverso: **`Conversation` passa a ser a linha** (perde um campo que a lista nunca leu, e o componente compila sem uma alteração) e o composto ganha nome novo no renderer, onde ele de fato mora. Forma da lição, boa para todo hook que a fase 15 e a 16 vão mexer: **quando a fonte de um hook muda, a compatibilidade do componente depende de qual dos dois tipos herdou o NOME — subtipagem estrutural entrega o componente de graça na direção certa e cobra um arquivo na direção errada.** Escolher a direção é gratuito antes de escrever a primeira linha e caro depois. [`plan/implemented/14-persistencia-das-conversas.md`](plan/implemented/14-persistencia-das-conversas.md)

### Decisão: o mock de armazenamento delega aos handlers reais, e não a uma fake escrita à mão
Os testes de nível 2 de conversa passaram a depender de um `window.api.conversation` que **lembra o que foi escrito** — sem isso, todo teste sobre trocar de conversa, renomear ou manter histórico fica vazio. A escolha foi entre uma fake em memória escrita à mão e delegar aos handlers reais contra um `:memory:`. Venceu delegar, pelo mesmo argumento que já sustenta o `satisfies Api`: **a fake teria de reimplementar ordenação por `updated_at`, o `COALESCE` que aplica título, a cascata e o merge de configurações — dezenas de linhas capazes de divergir dos handlers sem nada perceber.** Derivar em vez de duplicar significa que um teste de nível 2 verde está exercitando a regra de armazenamento que o app entrega. Custa uma propriedade que já existia: os handlers são funções puras recebendo o banco por parâmetro (DIP), então nada disso sobe o Electron nem envolve IPC. Contrapartida assumida e registrada: um defeito de handler agora deixa vermelhos também os testes do renderer, o que borra o diagnóstico — aceito porque a alternativa é um mock que mente. Confirmado por sonda antes de decidir: `node:sqlite` funciona sob o ambiente `jsdom` do Vitest, que continua sendo Node por baixo. [`plan/implemented/14-persistencia-das-conversas.md`](plan/implemented/14-persistencia-das-conversas.md)

### Decisão: TanStack Query adiado, e a régua é que decidiu
A intenção declarada era adotá-lo na primeira feature vertical. Aplicado o critério do próprio plano — *se eu adiar, quantos arquivos toco depois?* — a resposta foi dois hooks. As duas operações são mutações; o que a biblioteca entrega de fato (cache com chave, invalidação, deduplicação) não tem uso antes de existirem consultas repetidas sobre o mesmo dado. Registrado como exemplo de decisão revista pela régua, e não por preferência. **Gatilho** em [`ROADMAP.md`](ROADMAP.md). [`plan/implemented/06-primeira-feature.md`](plan/implemented/06-primeira-feature.md)

### Decisão: tokens em CSS custom properties, sem Tailwind — **revista em ago/2026**
Reversível de propósito, e vale registrar por quê. O `@theme` do Tailwind v4 resolve bem a fonte única, mas é dependência que afeta o build num projeto ainda validando a própria fundação. Como os tokens são custom properties, o Tailwind pode ser adicionado depois **lendo o mesmo arquivo**, sem reescrever token nenhum: adiar custa conveniência de escrita, não estrutura. Descartada sem volta a biblioteca de componentes (MUI, Chakra, shadcn) — trazem densidade e vocabulário de web, que é o oposto do que um app de desktop precisa. [`plan/implemented/05-design-tokens.md`](plan/implemented/05-design-tokens.md)

> **A reversão aconteceu, e a previsão se sustentou palavra por palavra:** o Tailwind entra lendo o mesmo `tokens.css`, sem um token reescrito, pelo `@theme inline`. Vale como caso raro de decisão adiada que **não** cobrou juros — o que a tornou barata foi ter escolhido custom properties, e não o adiamento em si. Detalhe e o que mudou junto: [`HISTORY-archive.md`](HISTORY-archive.md) § *Tailwind v4 entra*. A recusa a biblioteca de componentes **continua de pé**, e ficou mais forte: o `<dialog>` nativo da D13.8 já entrega o que os primitivos headless venderiam.

### Decisão: realce de sintaxe com `highlight.js`, e `shiki` descartado por acoplamento à CSP
`shiki` é a escolha óbvia e mais precisa (gramáticas TextMate, temas oficiais do GitHub), e é justamente por isso que o motivo de recusa precisa ficar escrito: ele **emite `style="color:#…"` inline**, o que acopla todo bloco de código ao `style-src 'unsafe-inline'` da CSP — a mesma diretiva que o [`ROADMAP § 2`](ROADMAP.md) lista como dívida a endurecer desde a fase 03. O dia em que alguém remover o `'unsafe-inline'`, o código perde a cor. Some-se o motor Oniguruma em WASM pedindo `wasm-unsafe-eval` no `script-src`. Os dois têm contorno (`transformerStyleToClass`, `createJavaScriptRegexEngine()`), e são dois contornos para chegar onde `highlight.js` já está: ele emite **classes semânticas** e nenhum estilo inline, o que mantém `tokens.css` como fonte única de cor e o `guard.mjs` satisfeito por construção. O que se paga: `highlight.js` faz correspondência de padrões, não parser completo, então JSX/TSX sai degradado — aceitável num app cujo assistente gera SQL e JSON. Forma da lição: **num app com CSP estreita, o formato de saída da biblioteca é critério de arquitetura, não detalhe de estilo.** [`plan/implemented/12-realce-de-sintaxe.md`](plan/implemented/12-realce-de-sintaxe.md)

### Decisão: hooks do Claude Code em `.mjs`, não `.py`
O mill.tools usa Python porque Python **é** a stack dele. Aqui seria a exceção: Node já é dependência obrigatória, Python não — e no Windows um `python` no PATH pode ser o stub da Microsoft Store, que abre a loja em vez de executar. Ganho concreto além disso: o `guard.mjs` lê o `tokens.css` do próprio projeto e valida `var(--token)` contra as declarações reais, o que a versão em regex não fazia. Os binários são resolvidos pelo campo `bin` do `package.json` de cada dependência e executados com o próprio Node — sem shell, o que elimina PATHEXT e aspas em caminho com espaço. [`plan/implemented/08-automacao-e-registro.md`](plan/implemented/08-automacao-e-registro.md)


---

## Armadilhas diagnosticadas

Mudaram de casa: [`ARMADILHAS.md`](ARMADILHAS.md). Motivo — armadilha se consulta **por sintoma**, não por data, então ela não obedecia ao corte cronológico deste arquivo e respondia por 63,7 KB dos 181,9 KB que ele tinha. Busque lá pelo termo do erro.

---

## Mudanças de ambiente

*Nenhuma registrada. As exclusões do Windows Defender aplicadas na máquina de desenvolvimento estão no [`CLAUDE.md`](../CLAUDE.md), porque são configuração viva e não histórico.*
